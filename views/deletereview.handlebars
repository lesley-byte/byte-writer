<section>
    <h1>Delete Review: {{reviews.title}} ?</h1>
    <p> {{reviews.review_text}}</p>
    {{log reviews}}
    <input type="hidden" name="reviewId" id="reviewId" value="{{reviews.id}}">
    <p>Are you sure you want to delete this review?</p>
    <form class="delete-review">
        <input type="hidden" name="reviewId" id="reviewId" value="{{review._id}}">
        <button class="delete-review-btn" type="submit">Delete</button>
    </form>
</section>

<script src="/js/delete-review.js"></script>